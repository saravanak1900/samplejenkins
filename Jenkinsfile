pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "Hello Europe!"
                sh "echo Hello from the shell"
                sh "hostname"
                sh "uptime"
            }
        }
        stage('Vacate india - work in UK') {
            steps {
                echo "Hello Europe!"
                sh "echo Enjoying whisky"
                sh "hostname"
                sh "uptime"
            }
        }

    }
}
