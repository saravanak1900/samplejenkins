pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "Hello Europe!"
                sh "echo Hello from the shell"
                sh "hostname"
                sh "uptime"
            }
        }
        stage('Prepare for Interview') {
            steps {
                echo "Hello Devops"
                sh "echo Enjoying AWS"
                sh "hostname"
                sh "uptime"
            }
        }
       stage('Tier2 Visa') {
            steps {
                echo "Hello Europe!"
                sh "echo Enjoying whisky"
                sh "hostname"
                sh "uptime"
            }
        }


    }
}
